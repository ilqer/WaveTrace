"""Domain layer: pure descriptions of the pipeline's shape, no I/O.

Currently holds only `contracts.py`; the rest of the `domain/application/adapters/entrypoints`
split (REFACTOR_PLAN.md §2) lands as later modules move here.
"""
